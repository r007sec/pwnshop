# Database Configuration
DB_HOST=127.0.0.1
# Change 'root' to your specific username if not using the default admin
DB_USER=root
# Leave blank for no password; do not use quotes
DB_PASSWORD=
DB_NAME=pwnshop

# AI Chatbot Configuration
GROQ_API_KEY=your_actual_key_here

# Server Configuration
PORT=3000
SESSION_SECRET=weak-secret-123
